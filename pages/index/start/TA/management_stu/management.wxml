<!--pages/management/management.wxml-->
<block wx:for-items="{{student}}">
    <view class="unit" bindtap="goToStudentInformation" id="{{item.stu_id}}">学号:{{item.stu_id}}姓名:{{item.name}}</view>
  </block>
